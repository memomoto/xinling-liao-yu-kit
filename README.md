# 心灵疗愈 · 独立前端

纯 **Vite + React** 的本地疗愈工具合集，侧栏入口由 `src/apps/registry.ts` 维护。

已移除：外链游戏、音视频播放器占位、壁纸/封面等装饰资源、桌面/侧栏桌宠、`public/home` 抓取的样式、以及旧版外链字体与短片流程。静态资源目录已收敛为绘本插画、少数图标与应用内实际引用文件。

### 常用命令

```bash
npm install
npm run dev
npm run build
npm run typecheck
```

`项目说明` 窗口展示本目录 `README.md`（源码内嵌）。
