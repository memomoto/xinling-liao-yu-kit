/**
 * macOS 风格占位尺寸 — 疗愈应用内拖拽与夹紧用（与本独立壳侧边栏宽度解耦）。
 * 桌面完整版曾与程序坞对齐；此处仅保留高度常量供小暖信箱等浮动元素使用。
 */

/** 顶栏 / 留白参考高度 */
export const MAC_MENU_BAR_PX = 32;

/** 底部区域预留（与原 Dock 占位一致） */
export const MAC_DOCK_PX = 150;

/** 本独立前端壳无 XP 右侧宠物竖条占位 */
export const MAC_RIGHT_SIDEBAR_PX = 0;

export const MAC_DOCK_GLASS_HEIGHT_PX = 52;
export const MAC_DOCK_ICON_LAYER_MIN_HEIGHT_PX = 148;
export const MAC_DOCK_ITEM_SLOT_PX = 76;
export const MAC_DOCK_FISHEYE_SIGMA_SLOT_RATIO = 0.82;
export const MAC_DOCK_FISHEYE_MAX_BOOST = 1.05;
